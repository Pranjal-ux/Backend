<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="h-screen bg-zinc-900 text-white">
      <div class="nav mb-3 p-3">
        <!-- <a class="text-red-500 font-semobold" href="/read"
          >Move it to the read page</a -->
        <!-- > -->
      </div>
      <h1 class="px-3 py-3 underline text-2xl font-mono">Update user</h1>
      <form class="py-2 px-5 flex flex-col" action="/update/<%=user._id%>" method="post">
        <input
          class="px-5 py-2 border-2 my-2 border-zinc-800 outline-none bg-transparent"
          type="text"
          placeholder="Enter the user"
          name="name"
          value="<%= user.name%>"
        />
        <input
          class="px-5 py-2 border-2 my-2 border-zinc-800 outline-none bg-transparent"
          type="email"
          placeholder="Enter the email"
          name="email"
          value="<%= user.email %>"
        />
        <input
          class="px-5 py-2 border-2 my-3 border-zinc-800 outline-none bg-transparent"
          type=""
          placeholder="Enter the image url"
          name="image"
          value="<%= user.image %>"
        />
        <input
          class="px-8 py-2 border-2 border-zinc-800 rounded-lg outline-none bg-blue-500"
          type="submit"
          placeholder="Create user"
        />
      </form>
    </div>
  </body>
</html>
