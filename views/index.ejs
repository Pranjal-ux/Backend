<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <!-- <h1>Addition <%= 2+2  %></h1> -->
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form text-white w-full">
        <form action="/create" method="post">
          <input
            class="first block mt-30 bg-zinc-800 w-full h-[50px] rounded-lg outline-none font-mono"
            type="text"
            placeholder="Title goes"
            name="title"
          />
          <textarea
            class="sec-text block mt-3 resize-npne bg-zinc-800 w-full h-[100px] rounded-lg outline-none font-mono"
            name="details"
            id=""
            placeholder="Write your task detail here!..."
          ></textarea>
          <input
            class="button bg-slate-900 font-mono rounded-xl w-full h-[10vh]"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="task flex justify-between wrap">
        <% if (files.length>0){ %> <% files.forEach(function(val){ %>
        <div class="task w-72 bg-zinc-800 rounded-lg">
          <h1 class="text-white text-2xl tracking-tighter">
            Some task goes here.
          </h1>
          <a class="link text-blue-500 inline-block" href="#">Read more</a>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-white font-mono underline">No task yet</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
